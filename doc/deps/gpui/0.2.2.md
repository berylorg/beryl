# gpui 0.2.2

- Verification date: 2026-05-14
- Enabled features in Beryl and normal reusable-crate builds: `default-features = false`, `windows-manifest`
- Additional dev/test feature used by `gpui-text-input` widget tests and Beryl composer geometry tests: `test-support`
- Baseline published source: crates.io package `gpui 0.2.2`
- Formal fork source commit: Zed `69e2130295c2649963eb639fc70b4f2ee8ea1624` (`gpui 0.2.2`)
- Workspace dependency declaration: `https://github.com/berylorg/zed-fork.git` at rev `fabbb607f5aff9e69ef6f09d423c5264bee4c9a8`
- Helper dependency declarations: `gpui-text-input` at rev `999929f8c11ca2202ea93cf5ef720c42343edaaf`; `gpui-settings-window` at rev `7c2867587ddd3eb014359a783de4117b4295043a`
- Local development override: ignored `.cargo/config.toml` may patch `gpui`, `gpui-text-input`, and `gpui-settings-window` to sibling local paths. Clean remote-only verification requires temporarily disabling that local patch.
- Fork branch: `gpui-fork`, created from the formal fork source commit
- Fork patch commits: `453a5657b2` (`Batch Windows glyph atlas uploads`); `f2193db331` (`Gate GPUI HTTP client feature`); `c70dbf5244` (`Release GPUI image atlas resources`); `fabbb607f5` (`Add source-backed GPUI image resources`)

## Why Beryl Uses It

Beryl uses `gpui` as the Rust-native desktop UI framework for the application shell in `crates/beryl-app`.

## Symbols Used By This Workspace

- Application and window lifecycle:
  - `Application`, `App`
  - `App::quit`
  - `Window::on_window_should_close`
  - `Subscription`
- Core element construction and layout:
  - `div`
  - `list`
  - `ListState`
  - `ListAlignment`
- Clipboard image and text handling:
  - `App::read_from_clipboard`
  - `App::write_to_clipboard`
  - `ClipboardItem`
  - `ClipboardItem::metadata`
  - `ClipboardItem::new_string_with_json_metadata`
  - `ClipboardEntry`
  - `ClipboardString`
  - `ClipboardString::metadata_json`
  - `ClipboardString::with_json_metadata`
  - `Image`
  - `ImageFormat`
- Image element rendering:
  - `img`
  - `ObjectFit`
  - `StyledImage::object_fit`
  - `Image::from_bytes`
- Native path prompts:
  - `App::prompt_for_paths`
  - `App::prompt_for_new_path`
  - `PathPromptOptions`
- Stateful div behaviors used in shell surfaces:
  - `Div::key_context`
  - `KeyBinding`
  - `actions!`
  - `InteractiveElement::on_action`
  - `Window::on_key_event`
  - `StatefulInteractiveElement::overflow_scroll`
  - `StatefulInteractiveElement::track_scroll`
  - `StatefulInteractiveElement::on_mouse_move`
  - `StatefulInteractiveElement::on_scroll_wheel`
  - `Window::request_animation_frame`
  - `ScrollHandle::bounds`
  - `ScrollHandle::max_offset`
  - `ScrollHandle::offset`
  - `Div::on_children_prepainted`
- List scrollbar geometry hooks:
  - `ListState::set_scroll_handler`
  - `ListState::viewport_bounds`
  - `ListState::max_offset_for_scrollbar`
  - `ListState::scroll_px_offset_for_scrollbar`
- Async coordination hooks:
  - `App::defer`
  - `Context::defer_in`
  - `Context::spawn`
  - `Context::subscribe`
  - `Context::emit`
  - `Window::defer`
  - `WeakEntity::update`
  - `Entity::update_in`
  - `AsyncApp::background_executor`
  - `BackgroundExecutor::timer`
- Window activation and creation:
  - `Window::activate_window`
  - `WindowOptions::show`
  - `WindowOptions::focus`
- Entity events:
  - `EventEmitter`
- Text input integration:
  - `EntityInputHandler`
  - `ElementInputHandler`
  - `UTF16Selection`
  - `Window::handle_input`
  - `Focusable`, `FocusHandle`, `Context::focus_handle`, `Window::focus`
  - `TextRun`, `TextSystem::shape_line`, `TextSystem::shape_text`, `ShapedLine`, `WrappedLine`
  - `WrappedLine::position_for_index`, `WrappedLine::index_for_position`, `WrappedLine::closest_index_for_position`, `WrappedLine::wrap_boundaries`
  - `PaintQuad`, `fill`, `Window::paint_quad`, `ContentMask`, `Window::with_content_mask`
- GPUI test support:
  - `#[gpui::test]`
  - `TestAppContext`
  - `VisualTestContext`
  - `TestAppContext::add_window_view`
  - `VisualTestContext::simulate_input`
  - `VisualTestContext::simulate_keystrokes`
  - `TestAppContext::read_from_clipboard`
  - `TestAppContext::write_to_clipboard`
  - `TestAppContext::did_prompt_for_new_path`
  - `TestAppContext::simulate_new_path_selection`

## Lifecycle And Threading Notes

- `gpui` owns the UI event loop and window lifecycle once the application shell is live.
- `Window::on_window_should_close` callbacks may cancel native window destruction by returning `false`; Beryl uses this for the main shell close path so the app event loop quits before the shell window is destroyed.
- `Subscription` cancels its callback when dropped; long-lived app observers must be stored or explicitly detached.
- Blocking filesystem, process, and protocol work must stay off the `gpui` thread, which matches the workspace design contract in `doc/design.md` and `crates/beryl-app/doc/design.md`.
- `Context::spawn` supplies a borrowed `AsyncApp`; async tasks that outlive the call site must clone that context before awaiting.
- `App::defer`, `Window::defer`, and `Context::defer_in` schedule work for the end of the current effect cycle so entities and windows currently on the stack can be returned before the callback runs.
- `WeakEntity::update` and `Entity::update_in` let Beryl drive later repaint-triggering state updates from async timers and cross-entity event handlers without calling layout-only window APIs out of phase.
- `gpui::list` is intended for differently sized items and caches measured item heights. When an item's height changes outside normal visible-area updates, the owning view must notify the list via `ListState::splice` or `ListState::reset`.

## Integration Gotchas

- Upstream `gpui` 0.2.2 enables `font-kit`, `wayland`, `x11`, and `windows-manifest` by default. Beryl disables upstream defaults and enables only `windows-manifest`.
- GPUI `test-support` enables additional dev/test dependencies and platform features such as `wayland` and `x11`; keep it in dev-dependencies rather than normal reusable-crate dependencies.
- The local GPUI fork makes the HTTP client dependency optional behind the `http-client` feature. Beryl's normal workspace resolution enables only `windows-manifest` for `gpui`; `cargo tree --workspace --edges features -i gpui` showed no `gpui/http-client` activation.
- Because `gpui/http-client` is not enabled in Beryl's normal workspace build, `zed-reqwest` and `ring` are not present in the normal/build dependency tree. `cargo tree --workspace --edges normal,build -i zed-reqwest` and the same command for `ring` both returned package-not-found.
- GPUI `test-support` in the local fork no longer enables `http-client`; HTTP-specific GPUI test helpers require the explicit `http-client-test-support` feature.
- Published `gpui 0.2.2` does not expose the local fork's `App::should_reduce_motion` or `App::caret_blink_interval` APIs.
- Activity-caret blink scheduling remains Beryl-owned and uses official GPUI async timer APIs. Beryl owns platform caret policy directly in `crates/beryl-app/src/shell/render/transcript/activity_caret.rs`.
- On Windows, Beryl reads text-caret blink policy through `GetCaretBlinkTime`; `0` and `u32::MAX` mean disabled blinking. Non-Windows currently uses Beryl's conservative 530 ms fallback.
- `Div::key_context` stores a `KeyContext` on the active dispatch-tree node during paint. Key binding resolution uses the focused node's dispatch path and context stack, so bindings can match focused descendants of the element that declares the context.
- `KeyBinding::new` binds a keystroke to an action within an optional context. Deeper matching contexts take precedence over ancestor contexts, and later-added bindings win at the same depth.
- `InteractiveElement::on_action` registers action listeners on the active dispatch-tree node. Bubble-phase action handlers stop propagation by default unless they call `cx.propagate()`.
- `Window::on_key_event` registers a low-level key listener on the active dispatch-tree node. Raw key listeners are useful for popup traversal and dismissal, but context-scoped key bindings plus actions are the direct route for focused input commands such as composer submission.
- `StatefulInteractiveElement::on_scroll_wheel` is filtered through `hitbox.should_handle_scroll(window)`, so overlay widgets that must block underlying scroll targets should occlude the area they cover rather than relying on hover semantics.
- `ListState::set_scroll_handler` is the right hook when Beryl needs post-scroll notifications from `gpui::list` without replacing the list's built-in wheel handling.
- `Window::request_animation_frame` is the clean repaint hook for view-local animation in official GPUI.
- `ClipboardItem::text()` concatenates only string entries and ignores image entries. Composer image paste must inspect `ClipboardItem::entries()` and match `ClipboardEntry::Image` before falling back to text.
- `ClipboardItem` exposes entries for inspection and owned iteration, but its public constructors cover single string entries, string entries with metadata, JSON metadata on a string entry, or a single image entry. Beryl can serialize custom editor selections such as composer image atoms as external plain text with `ClipboardItem::new_string_with_json_metadata("[Image A]".to_string(), metadata)`, then inspect `ClipboardItem::metadata()` or `ClipboardString::metadata_json()` on paste.
- `ClipboardItem::new_image(&Image)` stores `ClipboardEntry::Image(image.clone())`, and `Image` owns `bytes: Vec<u8>`. Beryl can avoid full image byte clones in transcript render-time menu targets and save-action state by keeping its own `Arc<[u8]>`, but the final GPUI clipboard handoff currently clones the GPUI image payload through the public API.
- GPUI 0.2.2 does not expose a cross-platform public API for writing multiple clipboard representations or custom MIME formats for one logical selection. Beryl should not rely on placing both image bytes and explanatory text into one portable clipboard item for composer atom copy; image bytes should stay in Beryl-owned process memory and clipboard metadata should contain only an opaque token.
- On Windows, GPUI clipboard reads choose one best-match entry. Clipboard images are exposed as `Image { format, bytes, id }`; file drops from the clipboard are exposed through string metadata, not image entries.
- On Windows, GPUI writes string metadata through private clipboard formats named `GPUI internal text hash` and `GPUI internal metadata`. `read_string_from_clipboard` returns metadata only when the stored hash still matches the visible `CF_UNICODETEXT` text, so Beryl should still validate visible clipboard text against any token-backed composer payload before restoring atoms.
- On Windows, GPUI clipboard writes use only the first entry in a `ClipboardItem`, so Beryl-owned clipboard writes that need deterministic external paste behavior should write the exact plain text representation they want external applications to receive.
- On X11, GPUI keeps the most recent owned `ClipboardItem` in process and returns it with metadata while the GPUI client still owns the clipboard; clipboard manager or external-owner round trips may return only plain text. Beryl composer paste must therefore treat metadata restoration as best-effort and use ordinary text paste when metadata is absent.
- `Image::from_bytes(format, bytes)` creates a GPUI image asset with a stable content-derived id and owns the encoded bytes in `Image { bytes: Vec<u8>, id }`. `img(Arc<Image>)` renders those bytes through GPUI's image cache, and `StyledImage::object_fit(ObjectFit::Contain)` fits the decoded image inside fixed popup bounds without stretching the aspect ratio.
- `ImageSource::Resource` accepts path-like resources, but the current `ImageAssetLoader` reads a path resource with `fs::read` into a `Vec<u8>` before decode and then returns `Arc<RenderImage>` through the same asset-output shape as byte-backed images. It does not accept a requested output size.
- `App::prompt_for_new_path(directory, suggested_name)` opens a native save destination picker and returns `oneshot::Receiver<anyhow::Result<Option<PathBuf>>>`. Picker cancellation resolves to `Ok(None)`. Picker-open failures resolve to `Err`, especially on Linux when the portal cannot open.
- The save picker supports an initial directory and suggested file name. It does not expose public file-type filters or a public default-extension option; callers that need an extension must include it in the suggested name and decide how to handle extensionless selected paths.
- The Windows save picker uses `IFileSaveDialog`; macOS uses `NSSavePanel`; Linux uses the xdg desktop portal save request. The API is public through `App` and re-exported by `gpui`.
- `EventEmitter<E>` is a marker trait for typed entity events. A view that implements it may call `cx.emit(event)`, and another entity can subscribe through `cx.subscribe(&emitter, handler)`. Beryl uses this for composer input marker-click events without making the shared text input depend on shell types.
- `Window::handle_input` must be called from an element's `paint` method with the focused `FocusHandle`; the registered input handler is frame-scoped and must be refreshed by painting.
- GPUI text input protocol ranges are UTF-16, while text layout and Beryl text models use UTF-8 byte offsets. Reusable text inputs must convert explicitly at the GPUI boundary and keep model operations on valid UTF-8 boundaries.
- `TextSystem::shape_line` is only suitable for single-line text. Reusable multiline input should use `TextSystem::shape_text` with a wrap width and use `WrappedLine` plus `wrap_boundaries` for visual-line geometry.
- Key dispatch tries action bindings first. If propagation remains active and the keystroke has a text character, GPUI forwards text to the active input handler. Text widgets should handle editing commands as actions and let ordinary typed characters arrive through `EntityInputHandler::replace_text_in_range`.
- `InteractiveElement::on_action` handlers stop propagation by default. A reusable text widget should call `cx.propagate()` only when a recognized action is intentionally left to host-owned behavior.

### Windows Window Show And Focus Reentrancy

- GPUI platform callbacks for request-frame, active-status, and input events enter window updates through app-borrowing paths. If a synchronous native Windows call reenters those callbacks while a GPUI update already owns the mutable app borrow, GPUI logs the failed borrow through `.log_err()`, which can appear as `RefCell already borrowed`.
- `Window::activate_window` is the public GPUI activation API. On Windows, GPUI schedules foreground activation work through its executor and uses `ShowWindowAsync(SW_RESTORE)` only when the target window is minimized.
- Code that owns raw Windows show/hide for a GPUI window should avoid synchronous `ShowWindow` from inside a GPUI window/entity update. Prefer `ShowWindowAsync` or a non-activating show command, then defer GPUI activation and widget focus with `Context::defer_in` or an async task after the current update unwinds.
- `WindowOptions::focus` may activate a window during creation. If a caller also needs app-owned field focus, creating the window unfocused and deferring activation plus field focus avoids a duplicate focused-open path.

### Windows Rendering Memory Baseline

- On Windows, `Application::new()` initializes the GPUI platform stack before Beryl application data is loaded. That stack includes OLE/windowing setup, DXGI/D3D11 device creation, DirectWrite text setup, executors, hidden helper windows, and GPU-driver modules.
- Opening a GPUI window creates renderer resources separate from Beryl-owned Rust data. The Windows backend uses DirectX renderer, DirectX atlas, DirectComposition/window resources, DirectWrite text rendering, swapchain-related resources, and vendor GPU driver allocations.
- GPU process-memory counters are useful for classifying this baseline, but their values must not be treated as Rust heap ownership. Process Private Bytes, Working Set, mapped module image size, and `\GPU Process Memory(*)\Total Committed` are related but not interchangeable.
- `ProcessModule.ModuleMemorySize` helps identify loaded rendering, text, shell, and GPU-driver modules. It is mapped image footprint and not a private-heap attribution mechanism.
- Release-profile Beryl runs are the relevant baseline for product memory investigation. Debug builds can carry additional GPUI debug and inspector paths, and the Windows renderer source probes D3D/DXGI debug support under debug assertions.
- Precise heap, GPU allocation, thread-stack, and mapped/private region attribution is not available from GPUI public APIs or ordinary PowerShell process counters. That level of attribution requires external profiling tools or additional targeted instrumentation.
- GPUI Windows atlas source in `platform/windows/directx_atlas.rs` uses a default atlas size of `1024 x 1024` and maximum atlas size of `16384 x 16384`. Monochrome glyph atlases use `DXGI_FORMAT_R8_UNORM`, while polychrome image/emoji atlases use `DXGI_FORMAT_B8G8R8A8_UNORM`.
- GPUI Windows renderer source in `platform/windows/directx_renderer.rs` uses a triple-buffered BGRA swapchain and full-window BGRA path intermediate textures, including a multisampled path target with `PATH_MULTISAMPLE_COUNT = 4`. On the measured `1560 x 1140` device-pixel window, local texture diagnostics estimated about `54 MiB` for swapchain plus path resources. Those resources explain the visible-window floor, not the additional normal-shell glyph/atlas delta.
- For memory profiling, distinguish atlas page growth, full-window path/swapchain resources, and driver/runtime allocations triggered by glyph atlas activity. Atlas page bytes are roughly `width * height * bytes_per_pixel` per page, but Phase 8 diagnostics showed the normal shell can add about `153 MiB` over `shell-minimal-render` while directly creating only one additional `1 MiB` monochrome atlas page.
- The Phase 8 follow-up entrypoints for Windows memory are `DirectXAtlas::get_or_insert_with`, `DirectXAtlasTexture::upload`, `ID3D11DeviceContext::UpdateSubresource`, `Window::paint_glyph`, `TextLayout::paint`, and DirectWrite glyph rasterization. The direct `CreateTexture2D` call sites alone are insufficient to explain the measured normal-shell excess.
- `DirectXAtlas::get_or_insert_with` checks the atlas cache, rasterizes on miss, allocates one tile, and uploads that tile. `DirectXAtlasTexture::upload` performs `UpdateSubresource` for a tile rectangle, so many first-frame atlas misses can mean many tiny D3D texture updates even when the process creates only one atlas page.
- Phase 9 diagnostics measured this directly: baseline normal shell performed `275` monochrome atlas uploads totaling only `37,154` bytes, while `shell-minimal-render` performed no atlas uploads and stayed about `154 MiB` lower in Private Bytes. `minimal-text-window` performed `5` uploads totaling `530` bytes and landed around `68.5 MiB`; `shell-textless-normal-geometry` performed `227` uploads totaling `32,298` bytes and still landed around `210.3 MiB`.
- Phase 10 confirmed the GPUI Windows fix target is upload-call policy, not atlas page size or payload byte retention. A diagnostic batcher kept the same `1,321` glyph paint calls, `275` atlas misses, and `275` tile insertions as baseline, but reduced atlas uploads from `275` calls to `2` calls. Private Bytes dropped from `216.836 MiB` to `66.949 MiB`.
- Batching must retain existing atlas contents. The invalid first diagnostic attempted full-page upload from a sparse dirty buffer and could erase previously uploaded glyphs, causing missing-letter artifacts. A correct implementation needs a persistent CPU mirror, tight dirty rectangles, or another representation that never uploads zeros over valid atlas regions.
- The production GPUI Windows fix should batch per-tile glyph uploads into one upload per dirty atlas texture or dirty region before renderer use, or otherwise avoid issuing hundreds of tiny `ID3D11DeviceContext::UpdateSubresource` calls during startup text paint.
- The formal fork patch in `gpui-fork` implements the production direction by keeping a CPU-side mirror per DirectX atlas texture, accumulating dirty bounds, and flushing the dirty rectangle before `DirectXAtlas::get_texture_view` returns the shader resource view. Fresh production-fork Beryl startup measured `66.859 MiB` Private Bytes and `42.551 MiB` VMMap `Private Data`.
- The CPU-side mirror intentionally trades a small amount of ordinary process memory for avoiding hundreds of tiny D3D atlas uploads. At the default `1024 x 1024` atlas size this adds about `1 MiB` for a monochrome glyph atlas and about `4 MiB` for a BGRA color atlas. This is acceptable for the measured startup path, but it is not a general bound for all atlas growth: GPUI's Windows atlas code can grow textures up to `16384 x 16384`, where a full BGRA CPU mirror would be about `1 GiB`. Future fork polishing should either cap mirror-backed atlas growth, switch large atlas pages to a sparse dirty-region representation, or otherwise avoid unbounded full-page CPU mirrors for large color atlases.

### Phase 3 Dependency Memory Retention Audit

- `App::loading_assets` is a process-lifetime `FxHashMap<(TypeId, u64), Box<dyn Any>>` of shared asset tasks. `App::fetch_asset` removes and reinserts a `Shared<Task<_>>`, so completed asset results remain cached until `App::remove_asset` is called. There is no item or byte cap. In the local fork, removing a recognized completed image asset also clones the decoded `Arc<RenderImage>` long enough to defer `App::drop_image`, releasing matching rendered image entries from live window atlases after any currently borrowed window is returned to the app.
- `RetainAllImageCache` is not an LRU cache despite its source comment. It is a `HashMap<u64, ImageCacheItem>` with no count, byte, decoded-pixel, or task cap. Its explicit lifecycle controls are `clear`, `remove`, and entity release, which call `App::drop_image` for loaded images. Beryl source inspection did not find `retain_all`, `image_cache`, or `.image_cache(...)` use sites, so Beryl's current `img(Arc<Image>)` paths use GPUI's app asset cache rather than this element cache. Beryl calls `Image::remove_asset` when transcript media entries or transcript image-preview popup images are evicted or closed; in the local fork this removes the GPUI asset task and, for completed image assets, releases uploaded image atlas entries through GPUI internals without a Beryl-side decoded-image handle.
- GPUI image decoding retains full decoded BGRA frames in `RenderImage`. GIF and animated WebP sources retain every decoded frame. `Image::from_bytes` owns encoded bytes and keys the GPUI asset cache by a content-derived id, so repeated render construction for identical bytes should hit the same GPUI asset entry, while distinct image bytes grow the app asset cache. `ImageAssetLoader` and `ImageDecoder` have no resize, frame-count, pixel-count, requested-render-size, or byte limit.
- The per-window sprite atlas retains glyphs, SVG alpha masks, and images. `Window::paint_image` inserts decoded image frames through `sprite_atlas.get_or_insert_with(AtlasKey::Image(RenderImageParams))` using `RenderImage::as_bytes(frame_index)`. On Windows, `DirectXAtlas` creates pages at least `1024 x 1024` and at most `16384 x 16384`, with one byte per pixel for monochrome pages and four bytes per pixel for polychrome pages. There is no total page count or total byte cap. The fork's CPU mirror is one full `Vec<u8>` per atlas page.
- `DirectXAtlas::remove` decrements a page-level live-key counter and drops a whole texture only when that page becomes unreferenced. It does not deallocate or reuse an individual removed tile while other keys remain live on the same page. `Window::drop_image` removes image frame keys, but GPUI exposes no equivalent public cleanup path for glyph or SVG atlas keys. Device loss and window drop clear the atlas.
- `TextSystem` has app-lifetime maps for font resolution, font metrics, and glyph raster bounds. `raster_bounds` is keyed by font id, glyph id, font size, subpixel variant, scale factor, and emoji flag. These maps have no count or byte cap and no public clear operation; Beryl can only limit churn by limiting distinct fonts, sizes, scale factors, and glyphs used during the app lifetime.
- `WindowTextSystem` owns a per-window `LineLayoutCache` with current and previous frame maps for unwrapped and wrapped line layouts. `finish_frame` swaps frames and clears the old current maps, so this is a two-frame layout cache rather than an app-lifetime text-history cache. It has no explicit byte cap, but stable UI text should plateau after the first rendered frames.
- `TextSystem::wrapper_pool` retains returned `LineWrapper` instances by `(FontId, font_size)`. Each wrapper has fixed ASCII width slots and an unbounded `HashMap<char, Pixels>` for non-ASCII character widths. The retained pool is bounded only by distinct font/size keys and peak concurrent wrapper handle count.
- `DirectWriteTextSystem` retains selected fonts in `fonts`, `font_selections`, and `font_id_by_identifier`. Custom fonts added through `add_fonts` update the DirectWrite custom collection, but GPUI exposes no font removal or cache-clear API. Color glyph rasterization creates temporary Direct3D layer textures, render-target/staging textures, and a constant buffer per rasterization call; those are not persistent caches, and persistent color glyph memory is the atlas tile uploaded afterward.
- `DirectXRenderer` owns fixed window resources plus high-water pipeline buffers. Swap chains use `BUFFER_COUNT = 3`. Path rendering owns one BGRA intermediate texture and one BGRA MSAA texture with `PATH_MULTISAMPLE_COUNT = 4`, sized to the current window and recreated on resize. Per-pipeline structured buffers start at fixed item counts (`shadow=4`, `quad=64`, `path_rasterization=32`, `path_sprite=4`, `underline=4`, `monochrome_sprite=512`, `polychrome_sprite=16`) and grow to `next_power_of_two` of the largest scene batch seen; they do not shrink until renderer drop or device-loss recreation.
- Window/app scratch structures clear after frames but generally retain allocation capacity: the thread-local element arena starts at `1 MiB` chunks and keeps any chunks added for a large frame; `Frame::clear`, `Scene::clear`, `layout_id_buffer.clear`, and `TaffyLayoutEngine::clear` clear contents but do not provide deterministic byte caps. First-window rendering can therefore look like growth from startup even when it is the fixed high-water allocation for a stable shell.
- Fixed startup cost that should not be mistaken for live-session growth includes `WindowsPlatform::new` OLE setup, the hidden platform message window, shared DXGI/D3D11 device/context creation, DirectWrite setup, the first visible `DirectXRenderer`, shader/pipeline creation, swap-chain/path textures, first-frame text shaping, first atlas pages, and renderer buffer warm-up. A Phase 4 measurement should separate post-first-idle plateau from repeated creation of new image bytes, fonts, glyph sizes, SVG sizes, or peak-size scenes.

### Phase 8 Decoded Image Diagnostics

- Verification date: 2026-05-13
- The local GPUI fork now exposes decoded-image and renderer-window readiness metadata through the existing `App::renderer_diagnostic_snapshot` path, without adding a new dependency or changing Beryl's normal GPUI rendering boundary.
- `RendererDiagnosticSnapshot` includes `decodedImageAssets`, a bounded metadata-only snapshot of recognized GPUI image asset tasks. It reports retained image asset task counts, loading/completed/failed counts, completed decoded CPU byte estimates, frame counts, removal counters, bounded per-asset items, and truncation state.
- Completed decoded-image inspection is based on `Shared<Task<Result<Arc<RenderImage>, ImageCacheError>>>.peek()` for the image asset task types that GPUI's `img` element uses. Diagnostics do not poll pending asset futures, decode files, clone image bytes, or keep decoded `Arc<RenderImage>` handles beyond the borrowed metadata read.
- `Image::from_bytes` transcript media still enters GPUI as `ImageSource::Image -> AssetLogger<ImageDecoder>`. The asset key hash reported by GPUI is `hash(&Arc<Image>)`; Beryl visible-media diagnostics report the same `imageAssetKeyHash` for correlation.
- `App::remove_asset` records bounded removal counters for recognized image asset tasks before dropping the task entry. This distinguishes "never loaded" from "loaded then removed" at a counter level without retaining removed image bytes or handles.
- `App::remove_asset` also recognizes completed GPUI image asset tasks, clones the completed decoded `Arc<RenderImage>` only for cleanup, and defers `App::drop_image(image, None)` so rendered image atlas keys are removed after currently updating windows are back in `App.windows`. Pending, failed, missing, and already-removed image assets do not create renderer resources during removal.
- `RenderImage` exposes metadata helpers for diagnostics: decoded render id, optional frame size, and decoded byte-length estimate. These helpers read existing frame metadata and buffers without copying pixel data.
- `WindowRendererDiagnosticSnapshot` includes `surfaceUsable` and `surfaceUnusableReason`; Beryl adds shell-window readiness around the actual shell window id and treats inactive or zero-sized snapshots as not ready for renderer attribution.
- `Window::renderer_diagnostic_snapshot` returns a metadata-only snapshot for the current `Window` handle. Beryl uses this to merge the currently updating shell window into `App::renderer_diagnostic_snapshot`, because app-level snapshots can omit the window temporarily borrowed for the active update and otherwise report only another hidden or zero-sized window.
- Windows atlas diagnostics now include bounded per-image tile metadata for `AtlasKey::Image(RenderImageParams)`, including render image id, frame index, tile dimensions, estimated tile bytes, and truncation state.
- Focused validation included `cargo nextest run -p gpui --lib -E 'test(renderer_diagnostics_track_decoded_image_asset_lifecycle) or test(removing_completed_image_asset_drops_rendered_image_atlas_entries)'`, which covers zero retained assets, completed decoded assets, pending/failed/missing/already-removed assets, removal counters, and rendered image atlas release.

### Current Image Rendering Path Relevant To Source-Backed Work

- Verification date: 2026-05-14
- Current public byte-backed image construction is `Image::from_bytes(ImageFormat, Vec<u8>)`, which stores encoded bytes inside `Image` and derives image identity from the byte hash.
- Current path-backed image construction can flow through `ImageSource::Resource(Resource::Path(_))`, but `ImageAssetLoader` still reads the whole file into memory with `fs::read` before decoding. It does not stream, memory-map, or decode directly from a requested display size.
- Current decoded image output is `Arc<RenderImage>`. `RenderImage` stores BGRA frame buffers and exposes frame size plus decoded-byte diagnostic helpers; it is CPU-side decoded image data, not a GPU texture handle.
- Legacy `Image`, `RenderImage`, `Resource`, and custom-loader `img()` sources still use `Window::paint_image`, which inserts image frames into the per-window sprite atlas under `AtlasKey::Image(RenderImageParams)` and passes `RenderImage::as_bytes(frame_index)` as the source for atlas upload on a cache miss.
- Source-backed `ImageSource::File` and `ImageSource::Bytes` no longer route through a completed `Arc<RenderImage>` asset task for ordinary `img()` rendering. `Img::prepaint` requests a device-pixel render size, `Window::request_source_backed_image` spawns off-thread preparation, and `Window::source_backed_image_resource` consumes the temporary `PreparedImageUpload` into a standalone platform image resource.
- Source-backed image lifecycle state is window-owned in `window/source_backed_image.rs`. It tracks requests made in the current frame, source-backed resources referenced by the final frame scene, known render-size metadata by source, pending removals, and cumulative resource-removal counts. `Scene::replay` can copy prior `ImageSprite` paint operations without re-entering `Img::paint`, so `Window::reuse_paint` marks replayed image resources for budget accounting and `Window::draw` reconciles retention from the final `Scene` image-resource references. GPU removals are decided at frame finish but applied only after `Window::present` has submitted the scene, so the just-presented scene cannot reference a removed image texture.
- `Img::prepaint` skips source-backed image requests when the element bounds do not intersect the active content mask. This keeps clipped transcript media from decoding/uploading a texture that `Scene::insert_primitive` would immediately drop and the lifecycle would immediately evict.
- Source-backed image resources are bounded by per-window resource count and GPU-byte budgets. Over-budget resources are treated as transiently deferred, not as source failures, so a valid file-backed or byte-backed source can render later when budget pressure changes.
- Resources that leave the final scene-referenced working set are removed from the standalone platform image store. Pending decode/upload work for requests that left the working set is ignored because the request entry has been dropped before completion can publish a `PreparedImageUpload`.
- Renderer recreation or DirectX device loss can invalidate standalone image resources behind live window metadata. `PlatformImageResources::contains` lets the window detect stale live handles and reissue visible requests from `File(PathBuf)` or `Bytes(Arc<[u8]>)`.
- On Windows, standalone source-backed images are owned by `DirectXImageResources`. It creates one BGRA `ID3D11Texture2D` plus shader-resource view per image render request, uploads the temporary BGRA buffer with `UpdateSubresource`, and retains no CPU pixel mirror for those standalone resources.
- Current DirectX sprite drawing still binds one shader-resource view at shader slot zero in `draw_with_texture`. Standalone image sprites reuse the polychrome sprite shader and draw one image texture per image batch rather than binding many image textures at once.
- Renderer diagnostics now separate decoded image asset bytes, standalone image resource GPU byte estimates, atlas image tile or CPU-mirror byte estimates, and window-owned source-backed image request lifecycle metadata. `WindowRendererDiagnosticSnapshot.sourceBackedImages` reports metadata-only request counts, pending decode/upload counts, pending-upload decoded CPU byte estimates, live GPU byte estimates, current-frame request counts, final-scene resource-reference counts under the legacy `paintedResourceCount` field, and resource-removal counts; `PlatformRendererDiagnosticSnapshot.imageResources` reports the platform store's live resource count, upload counts, GPU byte estimates, and zero decoded CPU byte retention for standalone resources. Test renderer diagnostics implement the same separation without retaining pixel buffers for measurement.
- The legacy image-atlas release behavior added in fork revision `c70dbf5244` remains active: completed legacy image assets can release atlas entries when `App::remove_asset` recognizes the image asset task and deferred `Window::drop_image` removes the frame keys from `DirectXAtlas`. This removes image keys, but individual atlas tiles are not compacted; an atlas texture is dropped only when its page has no live keys.

### Phase 8 Source-Backed Image Live Verification

- Verification date: 2026-05-14
- `cargo build -p beryl --release` built the Beryl release artifact while the ignored `.cargo/config.toml` patch pointed the workspace at the sibling local GPUI fork.
- A diagnostic child launched from `target/release/beryl.exe` against an isolated copy of the Beryl home selected the local City Image Generation thread with 10 generated images visible.
- The image-visible snapshot reported 10 visible `native_generated_image` items with `backingKind = source_backed_file`; compressed bytes, decoded-byte estimates, image ids, and GPUI image asset key hashes were all absent for those visible media entries.
- The active renderer reported 10 live source-backed requests/resources, aggregate standalone image GPU bytes of `10,383,360`, zero pending decoded CPU bytes, zero platform decoded CPU bytes, zero decoded image asset bytes, and zero atlas image tiles. The individual standalone resources were downsampled to `624 x 416` BGRA textures for the measured window.
- Switching to a non-image thread released the standalone image working set: source-backed request/live/pending counts returned to zero, platform image resource count and GPU bytes returned to zero, and `evictedResourceCount` reached `10`. A 3-second idle-after-switch snapshot stayed at zero.
- Process private bytes moved from `77,340,672` baseline to `97,972,224` with the image thread visible and then settled around `81,440,768` after switch-away. These process counters include allocator, renderer, and driver effects, but the GPUI and Beryl logical-retention counters did not show retained decoded image bytes.
- This Phase 8 run used an ignored local path patch before the source-backed GPUI fork changes were committed. The Phase 12 publishing follow-up below records the remote dependency pins that now carry the same implementation in clean checkouts.

### Review Follow-Up

- Verification date: 2026-05-14
- A reviewer pass found that source-backed failure state could be cleared without spawning a retry, known/failed source metadata could persist after a source left the working set, generated savedPath media could be marked loaded after only dimension probing, and the dependency pin could not represent uncommitted local GPUI fork changes.
- `SourceBackedImageStore::request` now preserves failed-source state until a new request actually starts loading or the request leaves the working set.
- `SourceBackedImageStore::finish_frame` prunes `known_size_by_source` and `failed_sources` to sources still represented by retained requests or live resources, keeping lifecycle metadata bounded by the active source-backed working set.
- Beryl generated-image savedPath loading now performs direct-file full decode validation after dimension admission and before reporting a source-backed file as loaded. The decoded pixels are dropped immediately and are not retained as Beryl media state; GPUI still prepares the requested render size through the source-backed rendering path.
- Source-backed file `img()` elements in transcript media also install a fallback child, so a later GPUI-side file/decode failure can render a file-unavailable tile instead of a blank texture.
- The post-fix City Image Generation diagnostic rerun still reported 10 live source-backed resources and `10,383,360` live image GPU bytes while visible, with zero pending decoded CPU bytes, zero platform decoded CPU bytes, zero decoded image asset bytes, and zero atlas image tiles. After switch-away and idle, request/live/pending counts, known source count, failed source count, platform image resource count, and image GPU bytes all returned to zero.

### Phase 10 Scene-Reference Lifecycle Follow-Up

- Verification date: 2026-05-14
- Final-scene image references, not `Img::paint` callback execution alone, are now the retention authority for source-backed standalone image resources. `Scene` exposes image resource ids from both the finished scene and prior paint ranges; `Window::reuse_paint` marks replayed image sprites for budget accounting, and `SourceBackedImageStore::finish_frame` retains live resources referenced by the final scene.
- The diagnostic field `paintedResourceCount` remains for compatibility but now reports source-backed standalone resources referenced by the current frame scene.
- Source-backed `img()` prepaint skips requests outside the active content mask. In the City diagnostic window, the transcript presentation reported 10 generated media items, but only 4 images intersected the viewport/content mask; GPUI therefore retained and uploaded only those 4 standalone image resources.
- Live City verification against a release diagnostic child showed source-backed request/live/pending counts `4/4/0`, platform image resource count `4`, image GPU bytes `4,153,344`, upload count `4`, eviction count `0`, and zero retained decoded CPU image bytes after settling. A repeated renderer read did not change upload or eviction counters.
- Switching away from City released the 4 clipped-visible resources: request/live/pending counts and platform image resource count/GPU bytes returned to zero, and the source-backed eviction counter reached `4`.
- Switching back to City reloaded those 4 clipped-visible resources once. Moving the native cursor inside the diagnostic Beryl window, outside it, and back inside did not increase source-backed upload or eviction counters; upload count stayed `8`, eviction count stayed `4`, live count stayed `4`, and retained decoded CPU image bytes stayed zero.

### Phase 11 Source-Backed Image Preload Follow-Up

- Verification date: 2026-05-14
- `Window::preload_source_backed_image(source, request, cx)` is the public GPUI entry point for source-backed image preloads. Callers must use the same `ImageRenderSource` and `ImageRenderRequest` identity that a later visible `img()` would use.
- `SourceBackedImageStore` keeps one request map keyed by `ImageRenderRequestId`. Visible final-scene references remain the authoritative painted working set, while preload references are a separate lower-priority working set.
- Preload resources share the same standalone platform image store as visible source-backed resources. Visible resources are admitted first; preloads are admitted only inside the total source-backed budget plus the separate preload budget.
- The default preload budget is `32` standalone resources and `64 MiB`, named in source as `DEFAULT_SOURCE_BACKED_IMAGE_PRELOAD_RESOURCE_COUNT_LIMIT` and `DEFAULT_SOURCE_BACKED_IMAGE_PRELOAD_GPU_BYTE_LIMIT`.
- Ready preload upload buffers are either uploaded during the draw lifecycle or dropped on preload budget deferral, source exit, renderer clear, or stale completion. They are not retained indefinitely as decoded CPU pixels.
- A later visible `img()` request with the same `ImageRenderRequestId` reuses the preloaded resource without a second decode/upload.
- `ImageRenderRequest` canonicalizes requested source-backed render dimensions to an even device-pixel bucket before hashing request identity. This absorbs one-pixel layout snapping differences between an explicit preload and the eventual visible `img()` request before duplicate decode/upload work is queued.
- `SourceBackedImageStore` can also adopt an already-live same-source resource when the requested dimensions differ only by `SOURCE_BACKED_IMAGE_RESOURCE_REUSE_DEVICE_PIXEL_TOLERANCE`, currently `2` device pixels.
- Source-backed image diagnostics now include `finalSceneResourceCount`, `preloadRequestCount`, `preloadPendingDecodeCount`, `preloadPendingUploadCount`, `preloadLiveCount`, `preloadBudgetDeferredCount`, `preloadBudgetDeferralCount`, `preloadGpuBytesEstimate`, `preloadEvictedResourceCount`, `preloadMaxResourceCount`, `preloadMaxGpuBytes`, and per-request `retentionKind`.
- Beryl uses this API from transcript prepaint after list children are measured. It computes a preload band from the transcript viewport plus a half-viewport vertical margin and issues preload requests without widening the GPUI final scene or adding offscreen image elements.

### Phase 12 Remote Dependency Publishing Follow-Up

- Verification date: 2026-05-14
- The source-backed GPUI implementation was committed and pushed to `zed-fork` branch `gpui-fork` at `fabbb607f5aff9e69ef6f09d423c5264bee4c9a8`.
- `gpui-text-input` was updated and pushed at `999929f8c11ca2202ea93cf5ef720c42343edaaf` so its workspace `gpui` dependency uses the same `zed-fork` revision.
- `gpui-settings-window` was updated and pushed at `7c2867587ddd3eb014359a783de4117b4295043a` so its workspace `gpui` dependency and transitive `gpui-text-input` dependency use the same published revisions.
- Beryl's root manifest now pins all three pushed revisions. With the ignored local `.cargo/config.toml` path patch temporarily disabled, `cargo tree -i gpui --locked` showed one `gpui v0.2.2` node at `fabbb607f5aff9e69ef6f09d423c5264bee4c9a8`, shared by `beryl-app`, `gpui-text-input`, and `gpui-settings-window`.
- Remote dependency verification passed for the helper crates and Beryl release build. The City Image Generation diagnostic run kept 10 standalone source-backed image resources during the visible/preload working set, retained zero decoded CPU image bytes after upload, kept upload and eviction counters stable across a normal scroll, and released all source-backed image resources after switching away.

## Minimal Upstream Entrypoints

- `gpui-0.2.2/Cargo.toml`
- local patched fork manifest: sibling `zed-fork/crates/gpui/Cargo.toml`
- `gpui-0.2.2/src/asset_cache.rs`
- `gpui-0.2.2/src/assets.rs`
- `gpui-0.2.2/src/gpui.rs`
- `gpui-0.2.2/src/app.rs`
- `gpui-0.2.2/src/app/context.rs`
- `gpui-0.2.2/src/app/async_context.rs`
- `gpui-0.2.2/src/app/test_context.rs`
- `gpui-0.2.2/src/executor.rs`
- `gpui-0.2.2/src/input.rs`
- `gpui-0.2.2/src/interactive.rs`
- `gpui-0.2.2/src/arena.rs`
- `gpui-0.2.2/src/scene.rs`
- `gpui-0.2.2/src/taffy.rs`
- `gpui-0.2.2/src/svg_renderer.rs`
- `gpui-0.2.2/src/elements/image_cache.rs`
- local fork source-backed image source/request model: sibling `zed-fork/crates/gpui/src/elements/image_source.rs`
- local fork source-backed image preparation: sibling `zed-fork/crates/gpui/src/elements/image_prepare.rs`
- `gpui-0.2.2/src/elements/list.rs`
- `gpui-0.2.2/src/elements/div.rs`
- `gpui-0.2.2/src/elements/img.rs`
- `gpui-0.2.2/src/keymap.rs`
- `gpui-0.2.2/src/style.rs`
- `gpui-0.2.2/src/text_system.rs`
- `gpui-0.2.2/src/text_system/line.rs`
- `gpui-0.2.2/src/text_system/line_layout.rs`
- `gpui-0.2.2/src/window.rs`
- local fork source-backed image lifecycle: sibling `zed-fork/crates/gpui/src/window/source_backed_image.rs`
- `gpui-0.2.2/src/platform.rs`
- `gpui-0.2.2/src/platform/mac/platform.rs`
- `gpui-0.2.2/src/platform/linux/platform.rs`
- `gpui-0.2.2/src/platform/windows/clipboard.rs`
- `gpui-0.2.2/src/platform/windows/direct_write.rs`
- `gpui-0.2.2/src/platform/windows/directx_atlas.rs`
- `gpui-0.2.2/src/platform/windows/directx_devices.rs`
- local fork source-backed image resources: sibling `zed-fork/crates/gpui/src/platform/windows/directx_image_resources.rs`
- `gpui-0.2.2/src/platform/windows/directx_renderer.rs`
- `gpui-0.2.2/src/platform/windows/events.rs`
- `gpui-0.2.2/src/platform/windows/platform.rs`
- `gpui-0.2.2/src/platform/windows/window.rs`

## Verification Inputs

- Commands run:
  - `cargo nextest run -p gpui --features test-support --test image_render_resources --test image_prepare --test image_sources`
  - `cargo fetch` in `gpui-text-input` with its ignored local Cargo path patch temporarily disabled.
  - `cargo nextest run` in `gpui-text-input`
  - `cargo fetch` in `gpui-settings-window` with its ignored local Cargo path patch temporarily disabled.
  - `cargo nextest run` in `gpui-settings-window`
  - `cargo fetch` in Beryl with the ignored local Cargo path patch temporarily disabled.
  - `cargo tree -i gpui --locked`
  - `cargo nextest run -p beryl-app --test transcript_media_sizing --test transcript_presentation_large --test diagnostic_dynamic_tools --test transcript_media_sources`
  - `cargo build -p beryl --release`
  - Beryl diagnostic child live run from `target/release/beryl.exe` against an isolated copied Beryl home, using `beryl_diagnostic.start`, `wait_for_state`, `switch_thread`, `scroll_transcript`, `read_renderer`, `read_memory`, `read_visible_media`, and `stop`
  - Beryl diagnostic child live run from `target/release/beryl.exe` against an isolated copied Beryl home, using `beryl_diagnostic.start`, `read_renderer`, `list_workspace_threads`, `switch_thread`, `read_memory`, `read_retained_state`, `read_visible_media`, and `stop`
  - `cargo nextest run -p beryl-app --test diagnostic_dynamic_tools --test diagnostic_child_dynamic_tools --test workspace_graph_dynamic_tools --test memory_diagnostics --test transcript_media_sources --test transcript_branch_menu --test transcript_media_sizing --test transcript_image_sources`
  - `cargo nextest run -p gpui --features test-support --test image_render_resources --test image_prepare --test image_sources`
  - `cargo nextest run -p gpui --test image_render_resources --test image_prepare --test image_sources`
  - `rg -n "name = \"gpui\"|gpui" Cargo.lock Cargo.toml crates/beryl-app/Cargo.toml`
  - `git -C ../zed-fork log --pretty=format:'%H %s' -n 4 c70dbf5244396cea7e627a73ffdcf45f19c9642d`
  - `rg -n "pub struct RenderImage|pub struct Frame|as_bytes|decoded|RenderImage" ../zed-fork/crates/gpui/src/assets.rs ../zed-fork/crates/gpui/src/platform.rs`
  - `rg -n "from_bytes|ImageDecoder|AssetLogger|ImageSource|ImageAssetLoader" ../zed-fork/crates/gpui/src/platform.rs ../zed-fork/crates/gpui/src/elements/img.rs`
  - `rg -n "paint_image|drop_image|get_or_insert_with|AtlasKey::Image|data.as_bytes" ../zed-fork/crates/gpui/src/window.rs ../zed-fork/crates/gpui/src/platform/windows/directx_atlas.rs`
  - `rg -n "DEFAULT_ATLAS_SIZE|MAX_ATLAS_SIZE|cpu_mirror|CreateTexture2D|UpdateSubresource|remove\\(|live_keys|dirty_bounds|get_texture_view|flush" ../zed-fork/crates/gpui/src/platform/windows/directx_atlas.rs`
  - `.cargo/config.toml` patch configuration was temporarily disabled for committed dependency verification.
  - `cargo tree --workspace --edges features -i gpui`; output showed only `gpui` feature `windows-manifest`, with no `gpui/http-client`.
  - `cargo tree --workspace --edges normal,build -i zed-reqwest`; output returned package-not-found.
  - `cargo tree --workspace --edges normal,build -i ring`; output returned package-not-found.
  - `cargo check --workspace --locked`
  - `cargo build --workspace --locked`
  - `Cargo.lock` was regenerated from the committed GitHub dependency revisions, not from ignored local path patches.
  - `cargo check --workspace --all-targets`
  - `cargo tree -i gpui`
  - `cargo tree -i gpui --workspace --all-targets`
  - `cargo tree -p beryl-app -i gpui`
  - `rg -n "forks/zed/crates/gpui|gpui = \\{ path|caret_blink_interval|should_reduce_motion" Cargo.toml crates doc`
  - `rg -n "pub fn should_reduce_motion|pub fn caret_blink_interval|fn should_reduce_motion|fn caret_blink_interval" <gpui-0.2.2>/src`
  - `rg -n "GetCaretBlinkTime|SystemParametersInfoW|SPI_GETCLIENTAREAANIMATION" <gpui-0.2.2>/src`
  - `rg -n "ClipboardEntry|ClipboardItem|new_image|struct Image|pub enum Clipboard" <gpui-0.2.2> -g "*.rs"`
  - `rg -n "ClipboardItem|ClipboardString|new_string_with_json_metadata|metadata|clipboard" <gpui-0.2.2>/src -g "*.rs"`
  - `rg -n "read_string_from_clipboard|text_hash|CLIPBOARD_METADATA_FORMAT|metadata" <gpui-0.2.2>/src/platform/windows/clipboard.rs`
  - `rg -n "D3D11|DXGI|DirectWrite|DirectComposition|Created device|Using GPU|D3D11_CREATE_DEVICE_DEBUG" <gpui-0.2.2>/src -g "*.rs"`
  - `Get-Counter '\\GPU Process Memory(*)\\*' -SampleInterval 1 -MaxSamples 1`
  - `Get-Process -Id <launched-beryl-pid> | Select-Object Modules`
  - Phase 3 source audit only; no Beryl GUI process was run.
  - `rg --files <zed-fork>/crates/gpui`
  - `rg -n "cache|Cache|cached|retain|HashMap|Lru|set_global|get_global" <zed-fork>/crates/gpui/src -g "*.rs"`
  - `rg -n "atlas|Atlas|glyph|texture|image|decode|upload|CreateTexture2D|UpdateSubresource|max|capacity|limit|clear|remove|evict" <zed-fork>/crates/gpui/src -g "*.rs"`
  - `rg -n "shape|Shaped|layout|Font|DirectWrite|TextSystem|FontCache" <zed-fork>/crates/gpui/src -g "*.rs"`
  - `rg -n "struct .*Cache|struct .*Renderer|struct .*Atlas|impl Drop|VecDeque|retain\\(|remove\\(|clear\\(" <zed-fork>/crates/gpui/src -g "*.rs"`
  - Targeted line reads of `<zed-fork>/crates/gpui/src/asset_cache.rs`, `assets.rs`, `app.rs`, `arena.rs`, `elements/image_cache.rs`, `elements/img.rs`, `platform.rs`, `scene.rs`, `svg_renderer.rs`, `taffy.rs`, `text_system.rs`, `text_system/line_layout.rs`, `text_system/line_wrapper.rs`, `window.rs`, `platform/windows/direct_write.rs`, `platform/windows/directx_atlas.rs`, `platform/windows/directx_devices.rs`, `platform/windows/directx_renderer.rs`, `platform/windows/platform.rs`, and `platform/windows/window.rs`.
  - `rg -n "gpui::(img|Image|retain_all|image_cache)|\\bimg\\(|Image::from_bytes|remove_asset\\(|drop_image\\(|retain_all\\(|image_cache\\(|use_render_image|get_render_image|ClipboardItem::new_image" crates Cargo.toml -g "*.rs" -g "*.toml"`
  - `rg -n "shape_line|shape_text|line_wrapper|resolve_font|TextRun|ListState|list\\(" crates -g "*.rs"`
  - `rg -n "set_global|default_global|global_mut|loading_assets|fetch_asset|use_asset|get_asset|Application::new|open_window|WindowOptions" crates -g "*.rs"`
  - `rg -n "prompt_for_new_path|prompt_for_paths|PathPromptOptions|file_save_dialog" <gpui-0.2.2>/src -g "*.rs"`
  - `rg -n "did_prompt_for_new_path|simulate_new_path_selection" <gpui-0.2.2>/src -g "*.rs"`
  - `rg -n "pub fn defer|fn defer|pub fn spawn|activate_window|ShowWindowAsync|update_window|try_borrow_mut" <gpui-0.2.2>/src`
  - `rg -n "WM_ACTIVATE|on_request_frame|on_input|on_active_status_change" <gpui-0.2.2>/src/platform/windows`
  - `rg -n "EntityInputHandler|ElementInputHandler|UTF16Selection|handle_input" <gpui-0.2.2>/src -g "*.rs"`
  - `rg -n "shape_text|TextRun|WrappedLine|position_for_index|closest_index_for_position|wrap_boundaries" <gpui-0.2.2>/src -g "*.rs"`
  - `rg -n "renderer_diagnostic_snapshot|WindowRendererDiagnosticSnapshot|window_handle" <zed-fork>/crates/gpui/src <beryl>/crates/beryl-app/src`
  - `cargo nextest run -p gpui --lib -E 'test(renderer_diagnostics_track_decoded_image_asset_lifecycle) or test(removing_completed_image_asset_drops_rendered_image_atlas_entries)'`
  - `rg -n "#\\[gpui::test\\]|TestAppContext|simulate_keystrokes|simulate_input|read_from_clipboard|write_to_clipboard" <gpui-0.2.2>/src -g "*.rs"`
  - `cargo nextest run -p gpui --test image_render_resources`
  - `cargo nextest run -p gpui --features test-support --test image_render_resources`
  - `cargo nextest run -p gpui --features test-support --test image_sources`
  - `cargo nextest run -p gpui --test image_sources`
  - `cargo nextest run -p gpui --test image_prepare`
  - `cargo nextest run -p gpui --features test-support --test image_prepare`
- Workspace files consulted:
  - `Cargo.toml`
  - `Cargo.lock`
  - `crates/beryl-app/Cargo.toml`
  - `crates/beryl-app/src/text_input.rs`
  - `crates/beryl-app/src/shell.rs`
  - `crates/beryl-app/src/shell/render/transcript.rs`
  - `crates/beryl-app/src/shell/render/transcript/activity_caret.rs`
  - `crates/beryl-app/tests/activity_caret_blink.rs`
  - external local GPUI helper crate source was consulted for input-pattern comparison; machine-local paths are intentionally omitted.
  - `doc/design.md`
  - `doc/plan.md`
- Formal fork baseline verification:
  - `.cargo_vcs_info.json` in the published crate names `69e2130295c2649963eb639fc70b4f2ee8ea1624` with `path_in_vcs = "crates/gpui"`.
  - `crates/gpui/Cargo.toml` at that commit has `version = "0.2.2"`.
  - The published `src/platform/windows/directx_atlas.rs` blob matches `crates/gpui/src/platform/windows/directx_atlas.rs` at that commit.
  - All published `src/platform/windows` files matched the selected commit by Git blob hash during the fork-baseline check.
