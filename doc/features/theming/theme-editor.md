# Theme Editor

This file is retained as a navigation pointer for older references.

The normative theme editor GUI composition contract is now in `gui.md`. The theming feature entry point remains `design.md`.
