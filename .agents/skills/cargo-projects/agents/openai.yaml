interface:
  display_name: "Cargo Projects"
  short_description: "Apply strict Cargo workspace project rules."
  default_prompt: "Use $cargo-projects when working in Rust Cargo workspaces, crates, Cargo.toml dependencies, lib.rs API docs, integration tests, nextest, or Cargo dependency investigation memory."
