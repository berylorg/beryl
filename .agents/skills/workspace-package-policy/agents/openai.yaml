interface:
  display_name: "Workspace Package Policy"
  short_description: "Apply workspace boundaries and filesystem rules."
  default_prompt: "Use $workspace-package-policy for workspace boundaries, package design and engineering-rigor declarations, filesystem policy, and source organization."
