interface:
  display_name: "Workspace Package Policy"
  short_description: "Apply workspace boundaries and filesystem rules."
  default_prompt: "Use this skill when working with workspace projects, subprojects, aggregating directories, package boundaries, system-doc contracts, .gitignore, ENV.md, or large source files."
